{
  "name": "@serviceos/api",
  "version": "0.1.0",
  "description": "ServiceOS API Server",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "node dist/server.js",
    "build": "tsc",
    "start": "node dist/server.js",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@serviceos/types": "workspace:*",
    "@serviceos/workflow-engine": "workspace:*",
    "@serviceos/agent-core": "workspace:*",
    "@serviceos/knowledge-graph": "workspace:*",
    "@serviceos/memory-system": "workspace:*",
    "@serviceos/compliance-core": "workspace:*",
    "@serviceos/shared": "workspace:*",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
