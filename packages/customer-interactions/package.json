{
  "name": "@serviceos/customer-interactions",
  "version": "0.1.0",
  "description": "Multi-channel customer interaction system for ServiceOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@serviceos/types": "workspace:*",
    "@serviceos/shared": "workspace:*",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/nodemailer": "^6.4.14",
    "typescript": "^5.3.2"
  }
}
