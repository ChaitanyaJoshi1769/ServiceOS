{
  "name": "@serviceos/plugin-system",
  "version": "0.1.0",
  "description": "Plugin ecosystem for ServiceOS",
  "main": "dist/index.js",
  "scripts": { "build": "tsc", "dev": "tsc --watch" },
  "dependencies": { "@serviceos/types": "workspace:*", "@serviceos/shared": "workspace:*" },
  "devDependencies": { "typescript": "^5.3.2" }
}
