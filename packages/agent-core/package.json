{
  "name": "@serviceos/agent-core",
  "version": "0.1.0",
  "description": "AI agent orchestration framework for ServiceOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@serviceos/types": "workspace:*",
    "@serviceos/memory-system": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "typescript": "^5.3.2"
  }
}
