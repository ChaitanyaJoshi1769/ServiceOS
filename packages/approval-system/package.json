{
  "name": "@serviceos/approval-system",
  "version": "0.1.0",
  "description": "Advanced approval and HITL workflow system for ServiceOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@serviceos/types": "workspace:*",
    "@serviceos/database": "workspace:*",
    "@serviceos/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
